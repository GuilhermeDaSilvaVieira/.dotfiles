return {
  client = {
    key = require("bindings.client.key"),
    mouse = require("bindings.client.mouse"),
  },
  global = {
    key = require("bindings.global.key"),
    mouse = require("bindings.global.mouse"),
    user = require("bindings.global.user"),
  },
  mod = require("bindings.mod"),
}
